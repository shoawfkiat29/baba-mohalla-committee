// Firebase web app config. Paste the firebaseConfig object from
// Firebase console → Project settings → Your apps → Web app.
// Until this is filled in, the app shows a "setup pending" screen.
window.FIREBASE_CONFIG = null;
